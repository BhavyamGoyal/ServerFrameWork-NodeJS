const users = require('express').Router();
//const multer = require('multer');


//var userController = require("../../API/DatabaseManager/UserController");

  // todoList Routes
  
users.get('/', );
users.post('/', );

users.get('/:userId', );
users.put('/:userId', );
users.delete('/:userId', );


users.get('/verificationStatus/:userEmailId', );
module.exports = users;


